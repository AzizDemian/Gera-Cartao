#include <stdio.h>

int main() {

    /*
    ATIVIDADE:

    Gere e imprima um número de cartão no seguinte formato:

    XXXX-XXXX-XXXX-DD

    Onde:
    - Cada X representa um dígito numérico (0 a 9).
    - DD representa dois dígitos verificadores (também de 0 a 9).
    - Os grupos devem ser separados por hífen (-).
    - O total deve conter exatamente 16 dígitos e 3 hífens.
    - O formato deve ser exatamente como mostrado, sem espaços extras.

    REGRAS IMPORTANTES:

    1) NÃO imprimir mensagens adicionais como:
       - "Seu cartão é:"
       - "Número gerado:"
       - Qualquer texto explicativo

       A saída deve conter APENAS o cartão no formato especificado.

    2) NÃO utilizar:
       - Loop infinito (ex: while(1), for(;;))
       - goto
       - system()
       - bibliotecas além de stdio.h

    3) O programa deve:
       - Compilar sem erros
       - Executar normalmente
       - Finalizar corretamente (sem travar)

    4) NÃO solicitar entrada do usuário.

    5) A saída deve ser exibida em uma única linha.

    Exemplo de saída válida:
    1234-5678-9012-34

    */

    return 0;
}
